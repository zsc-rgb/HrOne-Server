# 🧪 第2.3阶段 - 通用实体类测试指南

## ✅ 已完成内容总览

### 通用实体类模块 ✅
- `BaseEntity` - 基础实体类（174行）
- `PageDomain` - 分页参数（144行）
- `TableDataInfo` - 分页响应（122行）
- `BaseController` - Controller基类（103行）
- `PageTestController` - 分页测试接口（208行）

### 前后端联调配置 ✅
- `CorsConfig` - 后端跨域配置（60行）
- `vite.config.ts` - 前端代理配置
- `启动后端.bat` - 后端启动脚本
- `启动前端.bat` - 前端启动脚本

---

## 🧪 测试接口

### 分页测试接口总览

**访问：** `GET http://localhost:8080/test/page`

**响应示例：**
```json
{
  "total": 4,
  "rows": [
    {
      "name": "简单分页测试",
      "url": "GET /test/page/list",
      "description": "返回10条测试数据"
    },
    {
      "name": "大数据量测试",
      "url": "GET /test/page/bigList",
      "description": "返回100条测试数据"
    },
    {
      "name": "BaseEntity字段测试",
      "url": "GET /test/page/baseFields",
      "description": "查看创建时间、更新时间等字段"
    },
    {
      "name": "空数据测试",
      "url": "GET /test/page/empty",
      "description": "返回空列表"
    }
  ],
  "code": 200,
  "msg": "分页测试接口列表"
}
```

---

### 1️⃣ 简单分页测试

**接口：** `GET http://localhost:8080/test/page/list`

**功能：** 返回10条测试用户数据

**响应示例：**
```json
{
  "total": 10,
  "rows": [
    {
      "userId": 1,
      "userName": "用户1",
      "email": "user1@example.com",
      "phone": "13800000001",
      "age": 21,
      "createBy": "admin",
      "createTime": "2025-11-05 18:15:00",
      "remark": "测试用户数据"
    },
    {
      "userId": 2,
      "userName": "用户2",
      "email": "user2@example.com",
      "phone": "13800000002",
      "age": 22,
      "createBy": "admin",
      "createTime": "2025-11-05 18:15:00",
      "remark": "测试用户数据"
    }
    // ... 更多数据
  ],
  "code": 200,
  "msg": "查询成功"
}
```

**测试要点：**
- ✅ total 字段正确（10）
- ✅ rows 包含10条数据
- ✅ 每条数据包含用户信息
- ✅ code=200，msg="查询成功"

---

### 2️⃣ 大数据量测试

**接口：** `GET http://localhost:8080/test/page/bigList`

**功能：** 返回100条测试数据

**响应特点：**
```json
{
  "total": 100,
  "rows": [ /* 100条数据 */ ],
  "code": 200,
  "msg": "查询成功"
}
```

**测试要点：**
- ✅ total=100
- ✅ rows 包含100条数据
- ✅ 数据量较大时响应速度

---

### 3️⃣ BaseEntity 字段测试

**接口：** `GET http://localhost:8080/test/page/baseFields`

**功能：** 测试 BaseEntity 的所有字段

**响应示例：**
```json
{
  "total": 2,
  "rows": [
    {
      "userId": 1,
      "userName": "张三",
      "email": "zhangsan@example.com",
      "phone": "13800001111",
      "age": 25,
      "createBy": "admin",
      "createTime": "2025-11-05 18:15:00",
      "updateBy": "admin",
      "updateTime": "2025-11-05 18:15:00",
      "remark": "这是第一个测试用户"
    },
    {
      "userId": 2,
      "userName": "李四",
      "email": "lisi@example.com",
      "phone": "13800002222",
      "age": 30,
      "createBy": "system",
      "createTime": "2025-11-05 18:15:00",
      "updateBy": null,
      "updateTime": null,
      "remark": "这是第二个测试用户"
    }
  ],
  "code": 200,
  "msg": "查询成功"
}
```

**测试要点：**
- ✅ createBy 字段存在
- ✅ createTime 字段格式为 "yyyy-MM-dd HH:mm:ss"
- ✅ updateBy、updateTime 字段可选
- ✅ remark 字段存在

---

### 4️⃣ 空数据测试

**接口：** `GET http://localhost:8080/test/page/empty`

**功能：** 测试空列表的处理

**响应示例：**
```json
{
  "total": 0,
  "rows": [],
  "code": 200,
  "msg": "查询成功"
}
```

**测试要点：**
- ✅ total=0
- ✅ rows 为空数组
- ✅ 不会报错

---

## 📊 知识点总结

### 1. BaseEntity 设计模式

**为什么需要 BaseEntity？**

**优点：**
- **代码复用**：所有实体类继承后自动拥有通用字段
- **统一规范**：创建时间、更新时间等字段命名统一
- **维护方便**：需要添加新字段时只需修改一处

**实际应用：**
```java
// 用户实体
public class User extends BaseEntity {
    private Long userId;
    private String userName;
    // ... 业务字段
    
    // 自动继承：createBy、createTime、updateBy、updateTime、remark
}

// 角色实体
public class Role extends BaseEntity {
    private Long roleId;
    private String roleName;
    // ... 业务字段
    
    // 同样自动继承通用字段
}
```

---

### 2. 分页参数设计

**PageDomain 的作用：**
- 统一前端传递的分页参数
- 支持排序字段和排序方式
- 提供合理化查询功能

**前端传参：**
```
GET /user/list?pageNum=2&pageSize=20&orderByColumn=createTime&isAsc=desc
```

**后端接收：**
```java
@GetMapping("/list")
public TableDataInfo list(User user, PageDomain page) {
    // page.getPageNum() = 2
    // page.getPageSize() = 20
    // page.getOrderByColumn() = "createTime"
    // page.getIsAsc() = "desc"
}
```

---

### 3. 分页响应封装

**TableDataInfo 的作用：**
- 统一分页查询的响应格式
- 包含总记录数（用于计算总页数）
- 包含当前页数据列表

**前端使用：**
```javascript
// 前端收到的响应
{
  total: 100,    // 总共100条记录
  rows: [...],   // 当前页的数据
  code: 200,
  msg: "查询成功"
}

// 计算总页数
totalPages = Math.ceil(total / pageSize)
// = Math.ceil(100 / 10) = 10页
```

---

### 4. BaseController 设计模式

**为什么需要 BaseController？**

**优点：**
- **减少重复代码**：所有Controller继承后获得通用方法
- **统一响应格式**：使用 getDataTable() 统一分页响应
- **提高开发效率**：不需要每次都手动构建 TableDataInfo

**使用示例：**
```java
@RestController
@RequestMapping("/user")
public class UserController extends BaseController {
    
    @Autowired
    private UserService userService;
    
    // 方式1：简化写法（推荐）
    @GetMapping("/list")
    public TableDataInfo list(User user) {
        List<User> list = userService.selectUserList(user);
        return getDataTable(list);  // 自动构建 TableDataInfo
    }
    
    // 方式2：传统写法
    @GetMapping("/list2")
    public TableDataInfo list2(User user) {
        List<User> list = userService.selectUserList(user);
        TableDataInfo result = new TableDataInfo();
        result.setRows(list);
        result.setTotal(list.size());
        result.setCode(200);
        result.setMsg("查询成功");
        return result;
    }
}
```

---

## 📁 项目文件清单

### 本阶段新增文件

```
hrone-Server/
├── hrone-common/
│   └── src/main/java/com/hrone/common/
│       ├── core/
│       │   ├── domain/
│       │   │   └── BaseEntity.java           ✅ 基础实体类（174行）
│       │   ├── page/
│       │   │   ├── PageDomain.java           ✅ 分页参数（144行）
│       │   │   └── TableDataInfo.java        ✅ 分页响应（122行）
│       │   └── controller/
│       │       └── BaseController.java       ✅ Controller基类（103行）
│
├── hrone-framework/
│   └── src/main/java/com/hrone/framework/
│       └── config/
│           └── CorsConfig.java               ✅ 跨域配置（60行）
│
├── hrone-admin/
│   └── src/main/java/com/hrone/controller/
│       └── PageTestController.java           ✅ 分页测试（208行）
│
└── docs/
    ├── 第2.3阶段-通用实体类测试.md          ✅ 本文档
    ├── 前后端联调配置指南.md               ✅ 联调配置
    └── 前后端联调-快速开始.md              ✅ 快速上手

根目录/
├── 启动后端.bat                            ✅ 后端启动脚本
└── 启动前端.bat                            ✅ 前端启动脚本
```

### 代码统计

| 模块 | 文件数 | 代码行数 | 说明 |
|-----|--------|---------|------|
| BaseEntity | 1 | 174 | 基础实体类 |
| PageDomain | 1 | 144 | 分页参数 |
| TableDataInfo | 1 | 122 | 分页响应 |
| BaseController | 1 | 103 | Controller基类 |
| PageTestController | 1 | 208 | 分页测试 |
| CorsConfig | 1 | 60 | 跨域配置 |
| **本阶段合计** | **6** | **811** | - |
| **第2阶段总计** | **15** | **3136** | - |

---

## 🎯 学习成果

### 已掌握的设计模式

1. **继承模式**
   - BaseEntity 作为父类
   - 所有实体类继承通用字段
   - 减少代码重复

2. **封装模式**
   - PageDomain 封装分页参数
   - TableDataInfo 封装分页响应
   - 统一数据格式

3. **模板模式**
   - BaseController 提供模板方法
   - 子类Controller调用通用方法
   - 提高代码复用性

---

## 💡 实际应用场景

### 场景1：用户管理 - 分页查询

```java
@RestController
@RequestMapping("/user")
public class UserController extends BaseController {
    
    @Autowired
    private UserService userService;
    
    /**
     * 查询用户列表（分页）
     */
    @GetMapping("/list")
    public TableDataInfo list(User user) {
        // 使用 BaseController 的方法
        startPage();  // 设置分页参数
        
        // 查询列表
        List<User> list = userService.selectUserList(user);
        
        // 返回分页数据
        return getDataTable(list);
    }
}
```

---

### 场景2：用户实体类设计

```java
/**
 * 用户实体类
 * 继承 BaseEntity，自动拥有通用字段
 */
public class User extends BaseEntity {
    
    /** 用户ID */
    private Long userId;
    
    /** 用户名 */
    private String userName;
    
    /** 邮箱 */
    private String email;
    
    /** 手机号 */
    private String phone;
    
    // Getters and Setters...
    
    // 无需定义 createTime、updateTime 等字段
    // 这些字段已经在 BaseEntity 中定义
}
```

**数据库表设计：**
```sql
CREATE TABLE sys_user (
    user_id      BIGINT       PRIMARY KEY,
    user_name    VARCHAR(50)  NOT NULL,
    email        VARCHAR(100),
    phone        VARCHAR(20),
    
    -- BaseEntity 对应的字段
    create_by    VARCHAR(50),
    create_time  DATETIME,
    update_by    VARCHAR(50),
    update_time  DATETIME,
    remark       VARCHAR(500)
);
```

---

### 场景3：前端调用分页接口

**使用 Axios：**
```javascript
import request from '@/utils/request'

/**
 * 查询用户列表
 */
export function listUser(query) {
  return request({
    url: '/user/list',
    method: 'get',
    params: query  // { pageNum: 1, pageSize: 10 }
  })
}

// 在Vue组件中使用
const queryParams = {
  pageNum: 1,
  pageSize: 10,
  orderByColumn: 'createTime',
  isAsc: 'desc'
}

listUser(queryParams).then(response => {
  // response.total = 总记录数
  // response.rows = 数据列表
  this.userList = response.rows
  this.total = response.total
})
```

---

### 场景4：前端 Element Plus 表格

```vue
<template>
  <el-table :data="userList">
    <el-table-column prop="userId" label="用户ID" />
    <el-table-column prop="userName" label="用户名" />
    <el-table-column prop="email" label="邮箱" />
    <el-table-column prop="createTime" label="创建时间" />
  </el-table>
  
  <el-pagination
    v-model:current-page="queryParams.pageNum"
    v-model:page-size="queryParams.pageSize"
    :total="total"
    @current-change="getList"
    @size-change="getList"
  />
</template>

<script setup>
import { ref } from 'vue'
import { listUser } from '@/api/user'

const userList = ref([])
const total = ref(0)
const queryParams = ref({
  pageNum: 1,
  pageSize: 10
})

const getList = () => {
  listUser(queryParams.value).then(response => {
    userList.value = response.rows
    total.value = response.total
  })
}

// 初始加载
getList()
</script>
```

---

## 🔍 核心概念解析

### 1. 为什么 BaseEntity 要实现 Serializable？

**作用：**
- 支持对象序列化（存储到Redis缓存）
- 支持分布式系统间的对象传输
- 支持深拷贝

**示例：**
```java
// 将用户对象存储到Redis
User user = userService.getById(1L);
redisCache.setCacheObject("user:1", user);  // 需要序列化

// 从Redis获取
User cachedUser = redisCache.getCacheObject("user:1");  // 需要反序列化
```

---

### 2. @JsonFormat 注解的作用

**问题：** 
- Date 类型直接序列化为 JSON 时格式不统一
- 有的显示为时间戳，有的显示为默认格式

**解决：**
```java
@JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
private Date createTime;
```

**效果：**
```json
// 统一格式化为
"createTime": "2025-11-05 18:15:00"
```

---

### 3. params 字段的妙用

**问题：** 
- 实体类不想映射到数据库的临时字段怎么存储？
- 分页参数、时间范围等如何传递？

**解决：**
```java
// 前端传递时间范围查询
User user = new User();
user.getParams().put("beginTime", "2025-11-01");
user.getParams().put("endTime", "2025-11-30");

// Mapper XML 中使用
<select id="selectUserList" resultType="User">
    SELECT * FROM sys_user
    WHERE 1=1
    <if test="params.beginTime != null">
        AND create_time >= #{params.beginTime}
    </if>
    <if test="params.endTime != null">
        AND create_time &lt;= #{params.endTime}
    </if>
</select>
```

---

## 📖 前后端数据流

### 完整的分页查询流程

```
1. 前端发起请求
   ↓
   GET /user/list?pageNum=2&pageSize=10

2. 前端代理（Vite）
   ↓
   /dev-api/user/list → http://localhost:8080/user/list

3. 后端 Controller
   ↓
   public TableDataInfo list(User user, PageDomain page)

4. BaseController 处理分页
   ↓
   startPage()  // 设置分页参数

5. Service 查询数据
   ↓
   List<User> list = userService.selectUserList(user)

6. Controller 构建响应
   ↓
   return getDataTable(list)

7. 返回 JSON
   ↓
   { total: 100, rows: [...], code: 200, msg: "查询成功" }

8. 前端接收
   ↓
   渲染表格和分页组件
```

---

## ✅ 测试检查清单

### 后端测试 ✅
- [ ] 所有分页接口可以正常访问
- [ ] BaseEntity 字段正确序列化
- [ ] TableDataInfo 格式正确
- [ ] 日期格式统一为 "yyyy-MM-dd HH:mm:ss"

### 前端测试 ✅
- [ ] 可以通过代理访问后端接口
- [ ] 无跨域错误
- [ ] 响应数据格式正确
- [ ] 可以正确解析 TableDataInfo

### 联调测试 ✅
- [ ] 前端→代理→后端 流程正常
- [ ] 分页参数传递正确
- [ ] 响应数据前端可以正常使用

---

## 🎯 下一步学习

### 第2.4阶段：异常处理（预计2小时）
- [ ] BaseException - 基础异常类
- [ ] ServiceException - 业务异常
- [ ] GlobalExceptionHandler - 全局异常处理器

### 第2.5阶段：常量定义（预计30分钟）
- [ ] Constants - 通用常量
- [ ] UserConstants - 用户常量
- [ ] CacheConstants - 缓存常量

---

**🎉 恭喜完成第2.3阶段！** 

**当前进度：** 第2阶段已完成 60%（3/5）

**继续加油！** 💪

